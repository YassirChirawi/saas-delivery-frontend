export interface Restaurant {
  id?: string;
  name: string;
  ownerName?: string;
  whatsappPhone: string;
  active: boolean;
  imageUrl?: string;
  email: string;
  description?: string;
  address?: string;
}
